NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Required only to enable /api/internal/apply-* migration routes (optional — prefer Supabase SQL Editor)
MIGRATION_RUN_KEY=generate-a-long-random-secret

# Required only if using migration HTTP routes (not needed for normal app runtime)
# POSTGRES_URL=postgresql://...
